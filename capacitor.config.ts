import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'code4good.mobile.app',
  appName: 'Code4GoodApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
